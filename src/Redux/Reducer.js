
export default function reducer(state, action) {
    if (action.type == "rt") {
        let id = localStorage.getItem("id")
        return id;
    }

    else {
        let id = localStorage.getItem("id")
        return id;
    }

}